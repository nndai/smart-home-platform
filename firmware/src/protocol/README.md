# Binary Protocol Module (`firmware/src/protocol`)

Module chứa toàn bộ bộ giải mã & đóng gói giao thức nhị phân (TLV binary protocol) của hệ sinh thái Smart Home.

- Chi tiết kiến trúc, danh sách các class, hàm và hướng dẫn sử dụng: xem [docs/BINARY_PROTOCOL.md](../../docs/BINARY_PROTOCOL.md).
