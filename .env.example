# Copy sang .env rồi điền — KHÔNG commit file .env

# Supabase (P1)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# HiveMQ Cloud (chỉ để test tool, không commit credential thật)
HIVE_MQTT_HOST=
HIVE_MQTT_PORT=8883
HIVE_MQTT_USER=
HIVE_MQTT_PASS=
