[project]
name = "smart-home-platform-tools"
description = "Dev tooling: factory provisioning, bridge, flash"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
